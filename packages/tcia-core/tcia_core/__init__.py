"""tcia-core — shared primitives for sdr-engine and tcia-leadgen.

Placeholder. Real content lands in follow-up PRs as primitives are extracted
from sdr-engine (Cloudflare middleware, JWT verify, SQLite + alembic helpers,
A1 prompt-injection wrap, A2 cache-first atomicity, Codex retry-fallback,
Hermes workflow registry + checkpoint helpers, do_not_contact + audit_log
table primitives, cost-ceiling / circuit-breaker primitive).

See docs/leadgen-vision.md week-1 step #1 for the planned content list.
"""

__version__ = "0.0.1"
